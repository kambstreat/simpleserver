# requirements.txt
# This file lists all Python libraries your application depends on.
# Render will automatically install these when deploying.

Flask # The web framework
Pillow # For image processing
gunicorn # A production-ready WSGI HTTP server for Python apps (Render uses this)
